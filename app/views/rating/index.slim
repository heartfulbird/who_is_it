= content_for :main_page do

  #rating_page
    .span4
      h3.lobster.orange Рейтинг
    .span8
      /.pull-right.place.lobster У вас .. место в общем рейтинге
      .pull-right.place.lobster Рейтинг обновляется каждые 15 мин.

    .clearfix

    br

    .rating_info
      span.act.text
        | У вас&nbsp;&nbsp;
        span.position.lobster
        | &nbsp;&nbsp;место в общем рейтинге
      span.point
      span.post_rating.act Опубликовать рейтинг
      span.point
      a#invite.act onclick='VK.callMethod("showInviteBox");' Пригласить друга
    br
    br

    .users_desc
      table.table
        tr.orange_grad
          th Участник
          th Всего угадано
          th Место
          /th Статусы
            // У вас .. место в общем рейтинге
            // Опубликовать свой результат

        - @users.each_with_index do |u, i|
          tr.user id='#{u.id_vk}' data-i='#{i+1}'
            td
              span.u_foto = image_tag(u[:props]['photo']) if u[:props].present? && u[:props]['photo'].present?
              span.u_fio = u[:props]['fio'] if u[:props].present? && u[:props]['fio'].present?
            td
              span.u_count = u[:count]
            td = i + 1

            /td
            /  span.u_status медальки


        /tr
        /  td
        /    span class="u_foto"
        /      img alt="Aowjj0dofw4" src="http://cs309330.vk.me/v309330167/362c/AowJj0DOFw4.jpg"
        /    span class="u_fio" Станислав Савицкий
        /  td 23
